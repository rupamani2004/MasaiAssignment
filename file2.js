// a) Arrow function isEven(n) that returns true if even, else false
const isEven = n => n % 2 === 0;

// b) Ternary operator for marks result
let marks = 42; // example value, you may change according to your input
let result = marks >= 35 ? "Pass" : "Fail";

// c) Arrow function greet(name) with ternary operator for default "Guest"
const greet = name => console.log(`Hello, ${name ? name : "Guest"}`);

aravind.alla@fionasolutions.com